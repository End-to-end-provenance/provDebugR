# pdf file
#pdf("plot.pdf")
#plot(c(1:5), c(11:15))
#title("plot.pdf")
#dev.off()

# case: prov dir found, but data not found.
#missing <- mtcars

# function node
fn1 <- function(x)
{
	return(x+2)
}

a <- fn1(5)